---
layout: default
---

<div class="news">
  <div class="grid">
    {% assign current_year = page.year %}
    {% assign year_news = site.news | where_exp: "item", "item.date | date: '%Y' == current_year" | sort: 'date' | reverse %}
    {% for item in year_news %}
      {% include news_item.liquid %}
    {% endfor %}
  </div>
</div>
